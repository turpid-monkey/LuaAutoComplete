language: java
jdk:
  - oraclejdk8

install:
  - ./gradlew --refresh-dependencies --stacktrace

script:
  - ./gradlew build

after_success:
  - ./gradlew cobertura coveralls

after_failure:
  - "cat ./build/test-results/*.xml"
